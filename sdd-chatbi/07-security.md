# 07. 安全设计

## 数据库

- 使用只读账号。
- SQL 解析后只允许单条 SELECT。
- 实施行级安全或强制租户过滤。
- 设置超时、行数、速率限制和审计。

## Python

- 不可采用课程代码的主进程直接 eval 或 exec。
- 必须在隔离容器或受限子进程执行。
- 禁止网络、shell、环境变量与敏感文件读取。
- 限制 CPU、内存、执行时长、输出大小和可用库。

## API 与 MCP

- 复用项目认证和授权。
- MCP 服务必须服务间认证、Origin 校验和网络隔离。
- 密钥仅由环境变量或密钥管理提供，日志脱敏。
