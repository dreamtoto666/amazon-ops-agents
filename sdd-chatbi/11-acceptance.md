# 11. 验收与测试

| 用例 | 预期 |
|---|---|
| 普通咨询 | 不调业务工具，直接回复 |
| 纯计算 | 进入受限 Python 路径，返回可验证数值 |
| 简单查数 | 读取 Schema，生成只读 SQL，正确回答 |
| 查数并作图 | 返回文字与可访问图表 |
| 连续追问 | 使用同一会话有效上下文 |

必须拒绝 DELETE、多语句 SQL、跨租户 SQL、网络/shell/系统文件 Python 请求、未认证请求和越权会话。

单个 MCP 故障时，应返回明确降级信息；图表失败不能丢失已验证的查数结果；每个请求必须有可检索请求 ID。
